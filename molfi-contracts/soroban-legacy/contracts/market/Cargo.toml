[package]
name = "molfi-market"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Prediction market lifecycle + resolution for Molfi"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
