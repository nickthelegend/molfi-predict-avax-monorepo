[package]
name = "molfi-privacy-pool"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Privacy-Pools commitment tree + nullifier set for confidential Molfi positions"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
