[package]
name = "molfi-musdc"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "mUSDC — Molfi test stablecoin with an open faucet (testnet)"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
